name: Release

# Two ways in:
#   * pushing a v* tag, which is the normal path
#   * manual dispatch with a ref, defaulting to the newest tag -- for
#     re-cutting a release whose build failed for reasons unrelated to the code
on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      ref:
        description: 'Tag or ref to build (blank = most recent tag)'
        required: false
        default: ''
      draft:
        description: 'Create the release as a draft'
        type: boolean
        required: false
        default: true

permissions:
  contents: write     # required to create the release and upload assets

jobs:
  resolve:
    name: Resolve ref
    runs-on: ubuntu-latest
    outputs:
      ref: ${{ steps.pick.outputs.ref }}
      version: ${{ steps.pick.outputs.version }}
    steps:
      # Full history: git describe needs the tags, and a shallow clone has none.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: pick
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.ref }}" ]; then
            REF="${{ github.event.inputs.ref }}"
          elif [ "${{ github.ref_type }}" = "tag" ]; then
            REF="${{ github.ref_name }}"
          else
            REF="$(git describe --tags --abbrev=0)"
          fi
          echo "ref=$REF" >> "$GITHUB_OUTPUT"
          echo "version=${REF#v}" >> "$GITHUB_OUTPUT"
          echo "Building $REF"

  package:
    name: Package ${{ matrix.name }}
    needs: resolve
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x64
            os: windows-latest
            # No generator named on purpose. Pinning "Visual Studio 17 2022"
            # breaks the moment the hosted image rolls to a newer Visual Studio,
            # with an error that reads as "no Visual Studio installed" rather
            # than "not that one". CMake picks the newest it finds.
            cmake_args: -A x64
            config: Release
         # - name: Linux x64
         #   os: ubuntu-latest
         #   cmake_args: -G Ninja -DCMAKE_BUILD_TYPE=Release
         #   config: Release

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve.outputs.ref }}
          fetch-depth: 0     # so the build can derive its own version from git

      - name: Install build dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            ninja-build \
            libx11-dev libxext-dev libxrandr-dev libxi-dev libxcursor-dev \
            libxfixes-dev libxkbcommon-dev libwayland-dev wayland-protocols \
            libegl1-mesa-dev libgl1-mesa-dev libasound2-dev libpulse-dev

      - name: Install NSIS (Windows)
        if: runner.os == 'Windows'
        run: | 
          choco install nsis --no-progress -y
          echo "C:\Program Files (x86)\NSIS" >> $env:GITHUB_PATH
        shell: pwsh

      # Self-diagnosing: if the generator ever fails to resolve again, the log
      # says what was actually on the machine instead of only what was missing.
      - name: Report toolchain (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cmake --version
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          if (Test-Path $vswhere) {
            & $vswhere -products * -format value -property displayName
            & $vswhere -products * -format value -property installationVersion
          } else {
            Write-Host "vswhere not found"
          }

      - name: Cache FetchContent sources
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: deps-${{ runner.os }}-${{ hashFiles('CMakeLists.txt') }}
          restore-keys: deps-${{ runner.os }}-

    
      - name: Configure
        run: >
          cmake -B build ${{ matrix.cmake_args }}
        

      - name: Build
        run: cmake --build build --config ${{ matrix.config }} --parallel

      # A release that fails its own self-test should not be published.
      #- name: Test
      #  working-directory: build
      #  run: ctest --build-config ${{ matrix.config }} --output-on-failure --timeout 300

      - name: Package
        working-directory: build
        run: cpack -C ${{ matrix.config }} --verbose

      - uses: actions/upload-artifact@v4
        with:
          name: package-${{ runner.os }}
          path: |
            build/OpticForge-*.exe
            build/OpticForge-*.zip
            build/OpticForge-*.tar.gz
          if-no-files-found: error

  publish:
    name: Publish release
    needs: [ resolve, package ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List artefacts
        run: ls -la dist

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve.outputs.ref }}
          name: OpticForge ${{ needs.resolve.outputs.version }}
          draft: ${{ github.event.inputs.draft != 'false' }}
          generate_release_notes: true
          files: dist/*
          body: |
            ## OpticForge ${{ needs.resolve.outputs.version }}

            
